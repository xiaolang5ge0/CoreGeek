# 《未来战争》v1.0 接口文档

# 刷新记录

| 版本 | 描述     | 时间       |
| ---- | -------- | ---------- |
| v1.0 | 初稿完成 | 2026-09-09 |

---

每局比赛将双方选手代码同时启动，选手代码通过httpserver方式启动运行并监听端口，判题系统通过http请求方式向选手程序发送当前地图状态数据并获取选手调度命令响应数据。

样例：bash run.sh port

说明：port为系统启动时传给选手程序的端口号，ip地址均为0.0.0.0

# 1 Request

## 1.1 顶层结构

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `roundNo` | int | 当前回合数 |
| `mapInfo` | MapInfo | 地图整体信息 |
| `teamOur` | TeamOur | 我方队伍全部信息 |
| `teamEnemy` | TeamEnemy | 敌方队伍可见信息 |
| `robot` | Robot | 当前场上机器人信息 |
| `phaseTask` | String | 当前已领取任务的原文描述 |
| `lastRoundRoleActionResults` | Map\<int, boolean\> | 上回合各角色动作执行合法性结果，key是角色ID，value=true合法，反之不合法 |
| `lastSummonTreasureResult` | int | 上回合 `summonTreasure` 结果码：`0`=未探测。指的是上一回合没使用`summonTreasure`动作，或者该动作非法；`1`=成功获取宝藏；`2`=无宝藏或宝藏暂未开启。指的是`targetPos`无宝藏或者没到宝藏开启时间；`3`=献祭物品错误。指的是传递的`item`中的物品错误，要求不能多、不能少，无顺序限制，否则都会报该结果码；4=宝藏已空 |
| `llmResp` | String | 上回合LLM 返回的响应内容 |
| `worldNews` | WorldNews | 包括官方消息与民间传闻 |
| `lastCmdResult` | String | 上回合 `executeCmd` 的执行结果，未发命令时为空字符串 `""`。格式约定：`"[exitCode:N]\n<输出>"`（N 为退出码）；超时 `"[TIMEOUT]\n<部分输出>"`；判题器侧异常 `"[JUDGER_ERROR]\n<原因>"`；输出超过 64KB 时末尾追加一行 `"[TRUNCATED]"` |
| `vendorShopList`             | Array                         | 小贩收购矿石清单与价格                                       |
| `weaponShopList`             | Array                         | 武器商店出售物品清单与价格，元素为 `{name: String, price: int}`，分别对应商品名称与价格 |
| `errors` | Error[] | 本轮产生的错误信息数组 |

## 1.2 MapInfo — 地图信息

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `width` | int | 地图 X 轴总宽度 |
| `height` | int | 地图 Y 轴总高度 |
| `zones` | Zone[] | 地图中立元素信息 |

### 1.2.1 Zone — 中立区域元素

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `pos` | Pos | 坐标点位 |
| `neutralType` | String | 中立元素类型，取值见下表 |

**neutralType 取值：**

| 值 | 说明 |
|----|------|
| `stone` | 石矿 |
| `iron` | 铁矿 |
| `copper` | 铜矿 |
| `vendor` | 小贩 |
| `weaponShop` | 武器商店 |
| `challengerTaskPoint1` | 挑战者任务点1 |
| `challengerTaskPoint2` | 挑战者任务点2 |
| `defenderTaskPoint1` | 防守者任务点1 |
| `defenderTaskPoint2` | 防守者任务点2 |

### 1.2.2 Pos — 坐标结构体

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `x` | int | X 坐标 |
| `y` | int | Y 坐标 |

## 1.3 TeamOur — 我方队伍信息

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `type` | String | 阵营标识，取值 `challenger`（挑战者）/ `defender`（防守者） |
| `teamId` | String | 队伍唯一 ID |
| `teamName` | String | 队伍名称 |
| `goldNum` | int | 队伍当前的金币数量 |
| `totalScore` | int | 队伍累计总积分 |
| `playerTasks` | PlayerTask[] | 全部任务点信息列表 |
| `roles` | Role[] | 我方所有单位列表 |

### 1.3.1 Role — 单位通用属性

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `id` | int | 角色 / 单位唯一编号 |
| `pos` | Pos | 单位所在坐标 |
| `roleType` | String | 单位分类，取值见下表 |
| `health` | int | 当前剩余血量 |
| `attackPower` | int | 攻击力数值 |
| `attackRange` | int | 攻击距离 |
| `backPackCapability` | int | 背包容量上限 |
| `backpack` | String[] | 背包内物品名称列表 |
| `level` | int | 等级。仅建筑持有，角色没有，初始等级1 |
| `cooldown` | int | 武器冷却剩余回合数，当前仅火箭发射台有冷却，加特林/电磁炮恒为0 |

**roleType 取值：**

| 值 | 说明 |
|----|------|
| `station` | 基地 |
| `gatling` | 加特林炮台 |
| `railgun` | 电磁狙击炮 |
| `rocket` | 火箭发射台 |
| `wall` | 围墙 |
| `pioneer` | 开拓者 |
| `worker` | 工人 |

注：双方基地大小为2*2，基地对应的pos传递的是左上角的坐标。

**角色 ID 分配规则：**

角色 ID 为全局唯一整数，由判题器在队伍初始化时分配，分配顺序固定。

| 阵营 | Challenger | Defender | 角色 |
|------|-----------|----------|------|
| worker1 | `10010` | `20010` | 工人 |
| pioneer | `10011` | `20011` | 开拓者 |
| worker2 | `10012` | `20012` | 工人 |
| station | `10013` | `20013` | 基地 |
| gatling | `10020`/`10021`/`10022` | `20020`/`20021`/`20022` | 加特林炮台 |
| railgun | `10030`/`10031`/`10032` | `20030`/`20031`/`20032` | 电磁狙击炮 |
| rocket | `10040`/`10041`/`10042` | `20040`/`20041`/`20042` | 火箭发射台 |
| wall | `40000`/`40001`/`40002`... | `41000`/`41001`/`41002`... | 围墙 |

### 1.3.2 PlayerTask — 任务点信息

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `taskType` | String | 任务类型，取值 `自进化类1` / `自进化类2` |
| `taskPosition` | Pos | 任务点坐标 |
| `coldDownRounds` | int | 任务刷新冷却剩余回合数，0表示当前任务点可接取任务，大于0表示任务点还有多少回合就绪 |
| `scoreReward` | int | 积分奖励 |
| `goldReward` | int | 金币奖励 |
| `isValid` | bool | 当前任务点是否允许领取任务。任务冷却中或任务点所有任务已做完均会使其为false |
| `timeoutRounds` | int | 任务超时回合数，任务超时后领取的任务会强制结束，并以之前提交过的通过率最高的答案计算积分与金币 |

## 1.4 TeamEnemy — 敌方信息

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `roles` | Role[] | 敌方单位信息 |

注：敌方基地与围墙位置全图可见，默认传递；敌方其他单位需进入己方视野范围才显示。

## 1.5 Robot — 机器人信息

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `roles` | RobotRole[] | 场上所有存活机器人信息，全图可见 |

### 1.5.1 RobotRole — 机器人单位

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `id` | int | 机器人唯一编号 |
| `pos` | Pos | 机器人当前坐标 |
| `roleType` | String | 机器人类型，取值 `smallRobot`（小型）/ `middleRobot`（中型）/ `largeRobot`（大型）/ `bossRobot`（BOSS） |
| `health` | int | 当前剩余血量 |
| `abnormalState` | String | 异常状态：被眩晕时为 `"dizzy"`，否则为空字符串 `""` |
| `targetTeam` | String | 机器人攻击的目标阵营：取值`"challenger"` 或 `"defender"` |

## 1.6 WorldNews — 世界消息

| 字段           | 数据类型 | 字段说明 |
| -------------- | -------- | -------- |
| `officialNews` | String   | 官方消息 |
| `folkLegends`  | String   | 民间传闻 |

## 1.7 Errors — 错误结构体

| 字段 | 数据类型 | 字段说明 |
|------|---------|---------|
| `errorCode` | int | 错误编码 |
| `description` | String | 错误文字描述 |

注：errorCode包含如下字段：

0-未知错误

1-任务超时

2-答案错误

3-网络错误

4-指令错误

5-LLM额度超限

注：

- 2-答案错误，即通过`submitAnswer`提交的答案不正确或者不完全正确。
- 5-LLM额度超限，每队每个游戏日（130回合） LLM 调用次数存在限制，在每个游戏日的第一个回合重置。当前每个游戏日只能调用3次 LLM，超出则会此错误。但是在自进化任务执行期间(接取任务到任务结束期间)不会受到此限制，并且在此期间调用LLM不会计入每个游戏日的LLM限制次数。

# 2 Response

## 2.1 顶层结构

| 字段 | 数据类型 | 说明 |
|------|---------|------|
| `roleCommandMap` | Map\<int, RoleCommand\> | 全角色指令集合。key = 角色 ID，value = 对应角色动作指令 |
| `prompt` | String | 提交给 LLM 的 prompt 内容，每个游戏日有调用次数限制，超出则会报错。详见上文对errorCode字段解释 |
| `executeCmd` | String | 沙盒环境中执行的命令，仅在执行任务期间才能使用 |

`executeCmd` 为选手提交到沙盒环境的命令。判题器会在本回合执行，执行时长不得超过15秒，否则视为执行指令超时，沙盒故障与命令超时均不计入队伍异常次数。

## 2.2 RoleCommand — 单角色指令

| 字段 | 数据类型 | 是否必选 | 说明 |
|------|---------|:---:|------|
| `action` | String | M | 执行动作标识，取值见下表 |
| `controllerId` | String | O | 操控武器攻击的角色ID，一个角色只能同时操控一座武器工事 |
| `targetPos` | Array\<Pos\> | O | 目标坐标数组。移动/建造/拆除/采集/使用/召唤宝藏/电磁狙击炮均只传1个目标位置；加特林/火箭发射台可传多个目标位置，目标位置数与当前武器等级数相同 |
| `name` | String | O | 操作对象名称 |
| `num` | Int | O | 购买 / 贩卖物品需要使用，表示购买n个物品，贩卖n个矿石，若不填则默认为1 |
| `taskAnswer` | String | O | 任务作答内容 |
| `item` | Array\<String\> | O | `summonTreasure` 献祭任务用品名称数组 |

## 2.3 动作码全集

| 动作码 | 说明 |
|--------|------|
| `move` | 移动角色，需指定目标坐标`targetPos`                       |
| `attack` | 操控武器攻击指定坐标，需指定目标坐标数组`targetPos` ，`controllerId`为控制武器的角色ID；仅夜晚可用，白天使用非法 |
| `sell`        | 卖出矿石换取金币，需指定物品名称`name`以及物品数量`num`，`num`不填则默认为1    |
| `buy` | 在武器商店购买商品，需指定物品名称`name`以及物品数量`num`，`num`不填则默认为1 |
| `build` | 建造围墙或武器工事，需指定建造建筑名称`name`与建造的目标坐标`targetPos` |
| `remove` | 拆除围墙，需指定想要拆除的围墙坐标`targetPos` |
| `acceptTask` | 领取任务，无额外参数 |
| `submitAnswer` | 提交任务答案，需指定提交的答案`taskAnswer` |
| `summonTreasure` | 献祭任务用品，召唤并开启祭坛宝藏；需指定 `targetPos`与献祭任务用品 `item`，`item`中的任务用品需要在背包中存在 |
| `use` | 使用背包中的消耗品 / 升级券，需指定待使用物品名称`name`，有些物品需要指定使用坐标`targetPos` |
| `drop` | 丢弃背包内指定物品，需指定物品名称`name` |
| `collect` | 采集矿石，需指定矿石坐标`targetPos` |

注：

- 对`summonTreasure`动作的额外补充说明：
  - 动作非法场景：`targetPos`不在开拓者周围一格，或者说`item`中的物品不存在于背包内，动作非法不会消耗任务用品。
  - 只要动作合法，不论是否开启祭坛，都会消耗掉任务用品。
- 响应报文例子详见`response.txt`。每个角色每回合至多提交 1 条指令， **`response.txt` 内同一角色 ID 重复出现，仅为列举全部动作变体的目录式写法，并非意味着同一回合一角色能够执行多条指令**。
