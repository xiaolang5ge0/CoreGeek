# 《未来战争》PROGRESS（待完成事项 · 活文档）

> **本文档 = 工程进度与待办清单**。每阶段完成/每次迭代后刷新。
> 配套：事实列表 `RULE_ASSUMPTIONS.md`、策略列表 `STRATEGY_DECISIONS.md`、日志分析 SOP `LOG_ANALYSIS_PLAYBOOK.md`。

## 总览（截至第六轮实战修复）

| 项 | 状态 |
|---|---|
| 当前阶段 | **P0~P5 + 十三轮实战修复完成；剩余 P6（宝藏/Replay/实验）与实战联调** |
| 测试 | **114/114 通过** |
| 打包 | `game/CoreGeek/dist/CoreGeek.tar.gz`（**tar 顶层 CoreGeek/ 目录**，平台父目录解包后运行 `<root>/CoreGeek/main3.py`） |
| 代码 | `game/CoreGeek/`，Python ≥3.11 纯标准库 |
| 当前行为 | Day1 双工人建满14墙+3火箭；夜1-3 双工人全力采矿卖钱（无蹲防）；开拓者单人控3炮；Day4+ 修墙岗（墙血<50%才修、正面优先、L3→WallFixer、L1/L2→升级券）；WallRegistry 追踪攻破/补建并优先重升级；武器优先升级；任务确定性+LLM兜底；选矿我方侧优先 |
| 遥测 | 每回合 stdout 精简加密 Request+Response+trace（密钥 12345678，`tools/decrypt_log.py` 解密） |
| 事实进展 | 用户确认+实战验证多条：蓝黄区(蓝1黄2)/昼夜起算/射程表/操控占用/单出生点/两批机器人/夜采夜卖合法/机器人主攻基地顺路杀工人/夜潮规模 |

## 阶段进度

| 阶段 | 内容 | 状态 | 关键验证 |
|---|---|---|---|
| P0 | 协议/合法性/异常兜底/遥测 | ✅ | 畸形输入→合法空响应；解包启动链路可复现 |
| P1 | Night1 生存（动态FRONT布局/A*/3火箭+墙） | ✅ | 仿真：Day1 建满14墙、三火箭、夜防零空转 |
| P2 | 经济闭环（MineLock/卖货/看门狗） | ✅ | 三验收（不半路跑/不抢占/不打转） |
| P3 | 单人夜防（火力轮转/致命优先/威胁估计） | ✅ | Night1 全歼基地满血；70只夜潮零伤 |
| P4 | 中期升级（券任务链/修墙回血/封矿黑名单） | ✅ | 升级序武器优先；墙防跳级 |
| P5 | 任务流（开拓者/LLM异步/沙盒异步/返程预算） | ✅ | 工程类零 LLM 完成；API 类 harvest 直采 |
| P5+ | 六轮实战修复（打包/墙向/夜经济/寻路/任务兜底） | ✅ | 100/100 tests；10天仿真 0 崩溃/基地存活/20任务零LLM |
| P6 | 宝藏主线/Replay调参/3R对比实验 | ⬜ 剩余 | — |

## 实战修复记录（2026-09-21 首场 PK 后）

| 问题 | 根因 | 修复 |
|---|---|---|
| 平台加载失败封号 | tar 缺顶层 CoreGeek/ 目录 | build_package 包裹顶层目录，复验平台场景 |
| 围墙建反 | 开口朝了来敌方向，炮塔直接挨打 | FRONT=背向图心（墙朝来敌）：challenger→西，defender→东 |
| 日志拿不到 | 平台只见 stdout | 遥测每回合 stdout 打印完整 Request+Response |
| 工人死锁 | 经济岗抢了石料岗唯一的石矿；空闲工人堵炮台/CP | 墙未建完经济岗不碰石矿；空闲自动让出布局格 |
| 任务求解器粗糙 | 直接 LLM 出计划，无分类无防线 | 按《自进化策略.md》重构 v2：LOCATE→分类→确定性修复/收割/LLM |
| acceptTask 重试风险 | FAIL 计 errorCode 4，重试循环=封号 | FAIL/未确认 → 终身回避该任务点 |
| 任务会话串场 | 同文本任务再接取时 session 未重置 | FSM 进入 TASK_WORK 时强制重置会话 |

## 事实落地变更（2026-09-21，用户确认 9 条）

| 事实 | 代码影响 |
|---|---|
| 蓝区=距基地1格建武器、黄区=距基地2格建墙 | LegalityGuard 新增区域校验；布局器升级（蓝区格被占/非法时自动迁移 CP 凑 3 炮）；模拟器同步执法 |
| 昼夜第 1 回合起算白天 | 无改动（原实现一致），状态升 CONFIRMED |
| 射程静态表+围墙数量上限 20 | 运行时 attackRange 优先不变；墙环 14 < 20 无影响 |
| 操控占用（不能边操控边移动） | 开拓者移动回合不开火、控炮回合不用药（已互斥） |
| 出生点全局单点每晚固定 | front_observed 遥测价值提升（一次观测终身有效） |
| 两批机器人分攻双方 | 火力目标加权 +5（优先打以我方为目标的） |
| 夜采夜卖合法、remove 夜间不建议 | 无行为变更（原本如此）；守卫把 remove 保守限白天 |

## 各阶段遗留（已记账，择机处理）

| 来源 | 遗留项 | 影响 | 处理计划 |
|---|---|---|---|
| P1 | FRONT 修正仅记录不重建（重建=拆3火箭25金/座） | 朝向错时墙浪费 | 实战证据充分后走证据驱动变更 |
| P2 | 石料工在墙建完后囤石头（卖价仅1金） | 轻微背包占用 | 顺路时捎卖，暂不弃疗 |
| P3 | 召回工人到位后仅待命（不修墙不用道具） | 回防价值未拉满 | P5 后接"应急道具/抢修" |
| P4 | 买错的升级券不回收（极小概率） | 占1背包格 | 经验证几乎不发生，暂不处理 |
| 全程 | 模拟器为简化裁判（机器人直线趋近、无碰撞细节） | 仿真≠实战 | 遥测+Replay 校准 |
| P5 | 任务求解器依赖赛事 LLM 质量；prompt 模板/探索命令序列为通用默认 | 任务通过率不确定 | 首场实战观察真实任务形态后迭代模板 |
| P5 | Multi Submit 频率保守（提交→错误→修正循环） | 极端任务下可能超时 | 观察真实通过率结算后调频 |

## 实战修复记录·第八轮（2026-09-22 issue#18/#19 复盘：自进化任务2未提交）

> 复盘方法见 `LOG_ANALYSIS_PLAYBOOK.md`；一键脚本 `py game/CoreGeek/tools/analyze_issue_log.py --issue 18 19`。

| 问题 | 根因（解密日志实锤） | 处理计划 |
|---|---|---|
| 任务2 拿到答案却不提交 | teamB r36 已算出 `total_count=10/world_heritage=6/oldest_era=周口店遗址`，r37/r38 开拓者只移动/建墙，无 `submitAnswer`；teamA 同 | **收割后强制提交**（拿到答案即合成提交命令），提交失败重试到 deadline |
| API 参数/认证横跳未收敛 | r26–r35 反复 `city`/`location`、`Bearer`/`X-API-Key`；teamA r33–r34 又用回 `X-API-Key` 得空结果 | 收敛纠错：命中 `200` 即锁定 认证×参数组合，不再回退 |
| `location=请阅读` 参数污染 | 把 `phaseTask` 文本（"请阅读task_1_beijing.md…"）当成城市候选 | 参数值禁取自 `phaseTask`/提示语，只取文档/响应中的真实字段 |
| 任务1 仍靠 LLM 三次 | 工程类确定性路径缺失（cat spec→改配置→去 CRLF 全靠 LLM） | 工程类 SOP 固化：find+cat spec → 按 spec 正则修复 → 去 CRLF → check |
| 两局总分均 88（仅任务1的80分） | 任务2 超时失败，无 +80 | 以上修复后复测通过率 |

## 实战修复记录·第十三轮（2026-09-22 issue#25 复盘：买券批量 + 修理工归位兜底）

> 复盘方法见 `LOG_ANALYSIS_PLAYBOOK.md`；issue#25 日志在正文+16 条评论里（`analyze_issue_log.py` 只读正文，需合并评论）。

| 问题 | 根因（解密日志实锤） | 修复 |
|---|---|---|
| **炮手只买 V1×1，不买 V2**（r280 有 272 金） | `_weapon_upgrade_cmd` 每次只算**当前目标券**；买一张即回去升级 | **`_shopping_needs`**：一次买齐当前所有待升武器所需券（V1×L1数 + V2×L2数）；在店内连续买直到买齐/买不起（issue#25 期望：V1+V2 一次带走） |
| **购买量可能发出非法 buy** | `_buy_qty/_voucher_qty/_stock_qty` 买不起时 `max(1,…)` 仍返回 1 | 买不起返回 0 + 调用处守卫 |
| **修理工从不回防、卡墙外**（r457–r471 `state=MINE_COLLECT_LOCKED, cmd=None`） | `_day` 里 `ctx.home_anchor` 在 **pioneer 分支**才设置 → 工人阶段恒为 None → `_path_home_len=0`、`_go_home(None)=None`，直接空转 | `ctx.home_anchor` 提到**工人决策前**；`_go_home` 失败兜底继续采集 |
| **归位慢 2-3 回合** | `_path_home_len` 用切比雪夫距离，低估绕墙路程 | 改用 **A\* 实际路径长度**（`find_path`） |
| **CP 被开拓者占用 → 归位 A\* 失败** | `find_path` 把被占的 CP 视为阻挡 | `_go_home` 退化到 `step_toward`（目标邻接格） |
| **测试** | — | 新增 3 例（一次买齐 V1+V2 / 买不起返回 0 / 修理工黄昏归位）；全量 **114/114 通过** |

## 实战修复记录·第十二轮（2026-09-22 用户策略：围墙升级/修复移到夜间）

| 变更 | 内容 |
|---|---|
| **升级时段重构** | `_upgrade_cmd` 拆分 `allow_use/allow_buy`：**白天只采购**（不占用采集），**夜间执行升级/修复**（升级=回血，省 WallFixer）；brain 白天+夜间都派墙任务（夜间可连续升级） |
| **夜间优先级** | 修理工夜间：①已分配升级任务 → ②受损<50%抢修 → ③无事才采矿（去掉 `repair_triggered` 门控） |
| **理由** | 用户：①白天最大化采集 ②升级=回血省修复包 ③机器人到墙有回合数，夜间升级来得及 |
| **测试** | 新增 `test_wall_upgrade_happens_at_night_not_day`（白天 0 次用券、夜间≥1 次）；全量 **111/111 通过** |

## 实战修复记录·第十一轮（2026-09-22 issue#23/#24 复盘：任务期 LLM 额度误用 + 健壮探索）

> 复盘方法见 `LOG_ANALYSIS_PLAYBOOK.md`；日志报告 `C:\Users\ZZL\AppData\Local\Temp\opencode\report_2324.txt`。

| 问题 | 根因（解密日志实锤） | 修复 |
|---|---|---|
| **任务2 读完 API_DOCS 后停摆**（两局共有，未发 curl/未提交） | **LLM 日限额被误用到任务期**：任务1 用光 Day1 的 3 次额度（teamB r15/17/19，teamA r14/16/18）→ 任务2 的 prompt 被 `_llm_budget_ok` 静默丢弃 → FSM 在空 `llmResp` 上连续 3 次非 JSON 后强制结束。接口文档 errorCode=5 明确"任务执行期间 LLM 不限次且不计数" | brain 任务分支**不再查日限额、不计数**；`_llm_budget_ok` 仅用于非任务 LLM |
| **teamB 任务1 失败（漏 cd）** | r17 LLM 命令 `sed ./check` 未带 `cd`，在默认目录执行 → `No such file`；无确定性兜底 | **命令锚定**：LLM 命令未显式 cd/非绝对路径 → 自动补 `cd "<工作目录>" && ` |
| **工程类 CRLF 失败（exit126）** | `./check: /bin/sh^M: bad interpreter`（check 脚本 CRLF），靠 LLM 自觉去 CRLF | **工程确定性探测**：定位 check → `sed -i 's/\r$//'` → chmod → `./check`；命令失败含 `bad interpreter/^M` → 自动转探测重试 |
| **多步 find→cat→find→cat 易失** | 每步 1 回合、依赖文件名提取与路径拼接 | **健壮探索（单命令，用户模板）**：定位任务文件 + 读同目录 README/API_DOCS/ws_*/spec.md + 列权限（`-printf '%p %m'`） |
| **TOKEN 提交依赖 LLM** | check 通过输出 `TOKEN: xxx` 仍需 LLM 解析 | **TOKEN 自动提交**：任意命令输出命中 `TOKEN:` → 合成 `{"token":...}` 直接提交（零 LLM） |
| 文件名提取仅 `.md` | 未覆盖 `.txt`/大小写 | 正则扩展 `.md|.txt`（`-iname` 大小写不敏感），失败回退 `task_*.md` |
| **测试** | — | 重写 `test_task.py`（10 例：健壮探索/额度耗尽仍发 prompt/工程零 LLM TOKEN/CRLF 重试）；全量 **110/110 通过**；`simulate_task.py` 更新为零 LLM TOKEN 流程 |

## 实战修复记录·第十轮（2026-09-22 用户补充：围墙状态表 + L3 修复口径 + 补建墙重升级）

| 变更 | 内容 |
|---|---|
| **WallRegistry（新 L2 模块）** | `src/agent/wall_registry.py`：Dict 维护每个墙位 `exists/level/health/is_front/breached_round/rebuilt_round`；识别"被攻破 / 补建"事件；`damaged()`(<50%,正面优先) / `upgradable()`(受损>补建>正面>其余) |
| **L3 修复口径** | 墙/武器到 L3 后升级券失效 → 夜间抢修 L1/L2 优先升级券（升级=回满血），**L3 只能用 WallFixer** |
| **修复阈值** | REPAIR_HP_RATIO=**0.5**（原 <0.999 任何掉血都修 → 改为 <50% 才修，省修复包） |
| **按需备货 WallFixer** | 升级器：Day3+ 至少 1；Day4+/有 L3 墙时按 L3 墙数备货（上限 3），按持有量补差；`_stock_qty` 支持目标数量 |
| **补建墙重升级** | 前夜被攻破→次日补建的墙回到 L1，升级队列**补建墙优先**（front_order 用 registry.rebuilt_round） |
| **不空蹲** | 修理工夜间无达标修复需求/无券时落回采矿（不再整夜待命） |
| **共享常量** | WALL_MAX_HP/WEAPON_MAX_HP/STATION_MAX_HP 上移到 protocol.py（单一真源，升级器 re-export） |
| **测试** | 新增 `tests/test_wall_registry.py`（10 例：攻破/补建、50% 阈值、补建优先、L3→Fixer/L1→券、按需备货、Day4 端到端）；全量 **106/106 通过** |

## 实战修复记录·第九轮（2026-09-22 任务求解器按 issue#21 重构）

| 变更 | 内容 |
|---|---|
| **任务求解器重写** | 严格按同事验证过的《自进化任务策略》：7 阶段 FSM（FIND→READ→LLM_LOOP→WAIT_CMD/WAIT_LLM→SUBMIT→DONE）+ LLM-JSON 协议（`{cmd,answer,isFinished}`）+ 文件递归读取 + SOP 自进化 |
| **容错** | 连续 3 次非 JSON 强制结束；错误回复回传；JSON 容忍解析；curl 分页提示 |
| **SOP** | 完成前 2 任务后提取，次日 prompt 附带匹配 SOP（跨任务经验复用） |
| **每日 LLM 限 3** | 跨天重置；任务求解也受此限 |
| 移除 | 旧的 LOCATE/分类/确定性 HARVEST 流程与 timeout/submit_rejected 引用 |
| 测试 | 重写 test_task.py（新协议：find→cat→LLM(cmd)→curl→LLM(answer)→submit；非 JSON 强制结束；容忍解析；SOP 提取） |

## 实战修复记录·第八轮（2026-09-22 角色固化 + 新闻经济 + 每日LLM限3）

| 变更 | 内容 |
|---|---|
| **角色固化** | 炮手(pioneer)/修理工(worker[0])/挖矿工(worker[1]) 按 ID 固定；各自优先级见策略表 §一.5 |
| **新闻经济** | 新增 `planners/news.py`：正则解析官方消息(矿种/停工/天数)；停工期优先卖、停工前囤货、恢复清空 |
| **每日LLM限3** | 新增 `LLM_DAILY_LIMIT=3`（跨天重置），任务求解也受此限 |
| **批量购券** | 武器/围墙升级券：`min(刚需, 背包容量, 金币//单价)`，扣除已持有不多买 |
| **夜间可行动** | 兵潮清空且天亮前余量 >30 → 开拓者出行动（任务/买券） |
| **city 取值bug** | phaseTask 指针文本被当城市名（`location=请阅读`）→ 修：从文件名 pinyin 映射 + 扩停用词 |
| 修 | 挖矿工被派建墙单却不执行（`_miner` 不处理 `self.build`）→ 卡 160 回合；已修 |

## 实战修复记录·第七轮（2026-09-22 issue#17 复盘：夜经济/选矿/升级）

| 问题 | 根因（解密日志实锤） | 修复 |
|---|---|---|
| 从不卖矿、没钱升级武器 | 背包一路涨到 65、金币卡 80 达 90 回合（专程卖要求小贩≤15，矿点远时永不触发） | 背包达 60% 立即去卖（不限距离）；专程卖距离 15→20 |
| 石头过量采集 | 墙已满 14 仍采石到 r95（经济岗锁定石矿不释放；批量固定 6 致剩 2 墙也采满） | 经济岗锁石矿即释放；批量=`min(6,剩余墙数)` |
| Day1 两工人都采石 | day1_rush 双工人共享同一石矿（互相抢矿更慢） | 改为 **1 号石料+建墙、2 号采矿+卖钱**（用户指定）；不共享矿；仅计算判断不够才双开 |
| 工人空转 | 铜铁耗尽+石矿被锁 → 经济岗 idle | 经济岗石矿兜底；sim 矿刷新贴近真实 |
| 升级吃掉金币不升武器 | 廉价墙券(20)先于武器券(100)消耗；墙 L2 任务未加武器门控 | 武器未 L2 时暂停墙升级（仅修临界受损）；墙全 L2 任务补门控 |
| 两个工人都在做升级 | 升级任务同时派给两个工人 | 每回合最多 1 个升级任务；武器升级可抢占采矿 |
| 夜间避让方向错 | 撤向基地=迎着兵潮方向 | 基地安全→直接远离机器人；基地危险→才撤内圈（用户指出） |
| 选矿定义 | 仅按基地距离 | 改 **B 方案**：以"机器人行军走廊"定义安全矿（用户选 B） |
| 任务兜底脆弱 | LLM 响应仅取首行；无答案到点放弃；无 submit 重试 | 参考 issue#16：全段搜索/答案守卫/引号状态机/submit 重试2次 |

## 实战修复记录·第六轮（2026-09-22 自演练复盘：夜晚经济/寻路/任务兜底）

| 问题 | 根因 | 修复 |
|---|---|---|
| Night1 两工人回防震荡、不采矿 | `repair_worker` 因"任何墙掉血"就启用；`night_home` 是整夜粘性锁 | 修墙岗仅 Day4+/墙重伤才设；改滞回式危险规避(≤3进/≥5退)，脱离即采矿 |
| 机器人清空后仍蹲防不外出 | 粘性锁 + 修墙岗无墙可修时仍空蹲 | 修墙岗无需求返回 None 落回采矿；危险解除自动恢复 |
| 自进化1/2 任务失败 | LLM 兜底解析脆弱（仅取首行）；无答案时到点直接放弃 | LLM 响应全段搜索 ANSWER:/CMD:（兼容多行/前缀说明）；deadline-2 最后向 LLM 要最佳猜测答案 |
| 工人跑去敌方侧矿(右上)被杀 | `_select_mine` 只按距离选，我方矿耗尽后深入敌方侧 | 硬约束：优先我方基地≤16格；无近矿只接受我方侧；锁定后超距自动释放 |
| 墙被拆后重建 | —（验证：机制存在，100 tests 覆盖） | test_destroyed_wall_rebuilt_next_day |

验证：100/100 tests；完整10天仿真 0 崩溃、基地存活、20 任务零 LLM；夜采验证 0 次锁定敌方侧矿

## 实战修复记录·第五轮（2026-09-22 用户 4 点校准）

| 问题/要求 | 根因 | 修复 |
|---|---|---|
| Day1 只建 6-12 墙（缺 14 墙环） | `stone_finishable` 估算过于乐观（按1回合/墙），单工人实测只建~12 | Day1 双工人齐采石+建墙直到 14 墙环完成；build_phase 采够一批(6)连续建完再回矿 |
| 升级序错（墙全L2再武器L3） | 旧序把健康墙L2排在武器L3前 | 重排：武器L2→受损墙+FRONT墙L2→武器L3→墙全L2→墙L3；受损墙只升L1→L2，墙L3门控(无L1才升)杜绝 L3/L1/L1 相邻 |
| 夜间工人过度召回震荡 | 安全阈值过大(危险8/夜采10/走廊8)，但机器人主攻基地不绕路杀工人 | 阈值收窄：危险召回3(≈攻击射程)、夜采安全5、走廊4；事实入册 |
| 自进化 LLM 兜底不足 | 错误信息未系统累积送 LLM | TaskSession.error_log 累积去重错误，注入 LLM prompt；仅任务期调用(不限次,符合任务书) |
| API 任务认证/参数横跳 | Bearer/X-API-Key、city/location 反复试错耗尽15回合 | HARVEST v2 自动探测 认证×参数名×城市 直接合成 __ANSWER；确定性 Bearer/参数纠错 |

验证：92/92 tests + 完整10天仿真（Day1建满14墙、Day3无L3邻L1、武器[3,3,3]、基地升L2、score1600、20任务零LLM、0崩溃）

## 实战修复记录·第四轮（2026-09-22 三场 TeamA 日志复盘：issue #11/#12/#13 + 参考 #14）

| 问题 | 根因（解密日志实锤） | 修复 |
|---|---|---|
| 夜间工人墙边震荡不采矿（三场共有） | CRITICAL 群体召回过度触发：夜状态 CRITICAL×65 vs MINE×1 | 取消群体召回→个体危险召回(≤8格)+当晚锁定在家；夜1安全全力采集 |
| 炮台打对方机器人、漏打我方 | 火力评分 SELF_TEAM_BONUS=5 太弱 | 射程内有冲我方机器人时硬过滤只打我方的 |
| 自进化 API 反复失败 | 认证(Bearer)+参数(location)横跳耗尽15回合；HARVEST 只出候选不出答案 | HARVEST v2 直接合成 __ANSWER 零LLM提交；参数/认证确定性纠错；deadline-2 强制保底 |
| 采一个石头建一个墙 | STONE_BATCH 阈值低+建1墙后跌破阈值又回矿 | build_phase：采够一批(6)连续建完再回矿；预留回合兜底 |
| 墙单点跳 L3、正面墙没先 L2 | 升级序未按正面优先、无 L2 门控 | FRONT(离CP最远)优先；所有墙到 L2 前禁止升 L3 |
| 墙全程 L1、夜里没人修 | 无夜间修墙岗、券不在修墙工背包 | Day4+/墙受损固定 ID 最小工人为修墙岗，墙升级/备货优先派它(背包不共享自持物料) |
| 日志太大无法分析 | 全量明文 request | 遥测精简+XOR-SHA256加密(密钥12345678)+decrypt_log.py |
| worldNews 未留存 | 无存档 | 每回合存档 officialNews+folkLegends（去重），供后续宝藏推断 |
| Day1 双工人都采石、零金币 | day1_rush 让两人都进石料岗 | 改 1 石料+1 经济；仅"墙来不及建完"时经济工才临时帮建 |
| 武器 4 天不升级（金币被墙券吃光） | 廉价墙券(20金)优先消耗金币，永远攒不到武器券(100金) | 武器未到 L2 时为武器预留金币，仅修临界受损墙(ratio<0.3) |
| **stock 任务崩溃 78 次**（AttributeError .dump） | 备货任务 target=None/券名=str，trace 记录调 .dump() | 序列化前判类型；**异常=封号红线，已加回归测试守护** |
| 夜间修墙岗从不触发（repair 恒空） | 修墙工在墙内被机器人判定"危险"→召回分支抢先调走 | 修墙岗豁免危险召回（职责留墙内），仅贴脸(墙破)才闪避；Day4+/墙严重受损设岗 |

## 实战修复记录·第三轮（2026-09-21 三场日志复盘：issue #8/#9/#10）

| 问题 | 根因（日志实锤） | 修复 |
|---|---|---|
| 自进化 6 次仅 1 成 | 任务 timeout 仅 15 回合；API 参数名 location≠city；LLM 多行命令 bash 爆炸；工程类 spec 格式没认出 | 确定性参数纠错重试、CMD 消毒（拒多行/坏引号）、spec 正则补真实格式、deadline-2 强制交保底、harvest v2（认证×参数×城市自动探测） |
| Day1 墙太少（仅6块） | 单工人石料+矿远；经济岗先跑 | Day1 冲刺：回合<50 且墙<12 且有石矿 → 双工人采石建墙（可共享矿） |
| 夜采工人被兵潮打死 | 无机器人避让 | 矿/小贩安全半径10、工人危险圈8强制召回内圈、夜卖禁入危险区 |
| Day2 工人原地打转 | collect 失败无看门狗（矿空但 zone 在） | 连续3次 collect 背包不涨 → 解锁+拉黑20回合 |
| 墙全程 L1、基地从未修 | 只升受损<60%的墙，健康墙永不升 | 升级序重排：武器L2→受损墙→健康墙L2(20金)→武器L3→墙L3→基地；Day3+ 夜间修墙岗+WallFixer备货 |
| 日志太大无法分析 | 全量 request 回显 | 遥测 v2：精简结构（角色/机器人压数组、zones 仅变化时）+ XOR-SHA256 加密（密钥12345678）+ tools/decrypt_log.py |

## 实战修复记录·第二轮（2026-09-21 TeamB 日志复盘）

| 问题 | 根因 | 修复 |
|---|---|---|
| 98 回合起攻击全失效 | 友伤回避惩罚把贴脸机器人全判 -1000 哑火 | 删除友伤惩罚（U10 按实战关闭），回归测试贴脸必开火 |
| 正面墙 0 块基地被破 | 建墙按"离 CP 近"排序=侧面优先+brain 按"离工人近"派单 | 布局改为**迎敌侧最远端优先**，brain 严格按布局优先级派单 |
| 自进化1(API)失败 | harvest 的 URL 正则抓进中文/反引号→base 失效；服务端要 Bearer，LLM 反复 X-API-Key | URL 正则限 ASCII；新增**确定性 Bearer 纠错重试**（证据含命令+输出合并检索） |
| 自进化 spec 未识别 | 真实格式 `- logs/alpha/ 必须存在，权限为 755`、`第 3 行：\`port 8080\`` | _spec_fixes 增加两种真实格式正则 |
| 石料过少 | 石矿远+刷新慢 | 已记账：石料岗专职+批量阈值（进一步调参待下轮证据） |

## 待完成事项（优先级排序）

1. **实战联调**：按 `RULE_ASSUMPTIONS.md §11 必查清单`逐项核对（浪潮曲线 U1/出生点/机器人绕墙 U6/任务真实形态/异常率/时延）
2. **应急道具**：CRITICAL 时炸弹/眩晕的购买与使用（预算动用 RESERVE_GOLD）
3. **ReplayEngine**：读遥测 JSONL 离线复盘任意回合区间（"先 Replay 再实战"）
4. **P6 宝藏主线**：民间传闻累积推断献祭组合（任务期免费 LLM）
5. **任务求解器实战调优**：prompt 模板/探索命令默认序列/提交频率，依真实任务形态迭代
6. **3R vs 2R+1Q 对比实验**：需实战/Replay 数据支撑（EXPERIMENT）
7. **召唤令 PvP**：确认防御优势后开启（当前 CONFIRMED 关闭）
8. **工人夜采白名单**：U6 验证机器人路径后开放更激进的夜采

## 里程碑

- [x] M1：能跑通协议不死的 Agent（P0）
- [x] M2：能活过第一夜的 Agent（P1+P3）
- [x] M3：能自我造血的 Agent（P2+P4）
- [x] M4：能做任务拿分的 Agent（P5）
- [ ] M5：真实 PK 完成且 0 异常 ← 当前（已修复打包结构、墙向、夜经济、寻路、任务兜底；待实战验证）
- [ ] M6：全要素（宝藏/调参/实验）竞争级 Agent（P6）

## 维护约定

- **事实**（RULE_ASSUMPTIONS）：实战证据到达即更新状态（UNKNOWN→CONFIRMED/CONFLICT），改代码前先查事实表。
- **策略**（STRATEGY_DECISIONS）：与用户讨论的唯一入口；调参只改参数表；证据驱动流程 KEEP/REVERT/INCONCLUSIVE。
- **进度**（本文档）：每阶段闭环后刷新；遗留问题必须记账，不许口头遗忘。
- **防变形（每次迭代必做）**：改代码后立即用 `py -c "from agent import brain,fsm_worker,fire; ..."` 导出全部常量，逐条比对策略表参数，**不一致即改文档**；发现同一参数出现两处不同值时，删除旧条目（保留唯一真源）。
